__pycache__/
*.py[cod]
.pytest_cache/
.benchmarks/
.venv/
venv/
*.egg-info/
.DS_Store

# 빌드 산출물 중 대용량 인덱스는 커밋하지 않는다
knowledge/*.idx

# API 키는 절대 커밋하지 않는다
api_key.txt
.env
config.local.toml

# 빌드 중간 산출물 - webapp/bridge_guide.html 안에 이미 들어가 있다.
# 필요하면 python build/20_아티팩트_데이터.py 로 다시 만든다.
build/artifact_data.json

# Artifact 빌드 산출물 (게시할 때만 만든다)
webapp/bridge_guide.artifact.html

# "출처"에 쓰는 페이지 그림 - 서버가 필요할 때 렌더링해 캐시하는 것뿐이다
data/출처페이지/
